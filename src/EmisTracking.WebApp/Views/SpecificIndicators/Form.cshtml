@model EmisTracking.WebApi.Models.ViewModels.SpecificIndicatorViewModel

@{
    ViewData[Constants.GenerateBreadcrumb] = new();
}

@using EmisTracking.Localization

@section Breadcrumb
{
    <li class="breadcrumb-item active"><a asp-controller="SpecificIndicators" asp-action="Index">@LangResources.Titles.SpecificIndicatorsIndex</a></li>
    <li class="breadcrumb-item active">@ViewData["Title"]</li>
}

<form asp-controller="SpecificIndicators" asp-action="@ViewData["AspAction"]" asp-antiforgery="true" method="post">
    <div class="validation text-danger" asp-validation-summary="ModelOnly"></div>
    <input asp-for="Id" hidden="hidden" />

    <div class="row mb-3">
        <label asp-for="ConsumptionGroupId" class="col-md-2 control-form-label"></label>
        <div class="col-sm-6">
            <select asp-for="ConsumptionGroupId" class="form-control">
                <option value="" disabled selected>@LangResources.ChooseSomething</option>
                @if (Model.ConsumptionGroups?.Any() ?? false)
                {
                    @foreach (var item in Model.ConsumptionGroups)
                    {
                        <option value="@item.Value">@item.Name</option>
                    }
                }
            </select>
            <span asp-validation-for="ConsumptionGroupId" class="text-danger"></span>
        </div>
    </div>

    <div class="row mb-3">
        <label asp-for="PollutantId" class="col-md-2 control-form-label"></label>
        <div class="col-sm-6">
            <select asp-for="PollutantId" class="form-control">
                <option value="" disabled selected>@LangResources.ChooseSomething</option>
                @if (Model.Pollutants?.Any() ?? false)
                {
                    @foreach (var item in Model.Pollutants)
                    {
                        <option value="@item.Value">@item.Name</option>
                    }
                }
            </select>
            <span asp-validation-for="PollutantId" class="text-danger"></span>
        </div>
    </div>

    <div class="row mb-3">
        <label asp-for="Value" class="col-md-2 control-form-label"></label>
        <div class="col-sm-6">
            <input asp-for="Value" class="form-control" />
            <span asp-validation-for="Value" class="text-danger"></span>
        </div>
    </div>

    <div class="form-group row">
        <div class="col-sm-6">
            <input type="submit" class="btn btn-primary" value="@LangResources.SaveButtonText" />
        </div>
    </div>
    <br />
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left"></i>
        @LangResources.ReturnButtonText
    </a>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}